import { CrossfireFrame } from "../frame";
import { UnsupportedOrInvalid } from "./unsupported-or-invalid";
export declare class LinkStatisticsTx {
    /** uint8 - RSSI (dBm * -1) */
    rssiDb: number;
    /** uint8 - RSSI in percent */
    rssiPercent: number;
    /** uint8 - Package success rate / Link quality (%) */
    linkQuality: number;
    /** int8 - SNR (dB) */
    snr: number;
    /** uint8 - rf power in dBm */
    rfPowerDb: number;
    /** uint8 - rf frames per second (fps / 10) */
    fps: number;
    syncByte: number;
    constructor(
    /** uint8 - RSSI (dBm * -1) */
    rssiDb?: number, 
    /** uint8 - RSSI in percent */
    rssiPercent?: number, 
    /** uint8 - Package success rate / Link quality (%) */
    linkQuality?: number, 
    /** int8 - SNR (dB) */
    snr?: number, 
    /** uint8 - rf power in dBm */
    rfPowerDb?: number, 
    /** uint8 - rf frames per second (fps / 10) */
    fps?: number, syncByte?: number);
    static get payloadSize(): number;
    get frameType(): 29;
    get crossfireFrame(): CrossfireFrame;
    static fromFrame(frame: CrossfireFrame): LinkStatisticsTx | UnsupportedOrInvalid;
}
//# sourceMappingURL=link-statistics-tx.d.ts.map