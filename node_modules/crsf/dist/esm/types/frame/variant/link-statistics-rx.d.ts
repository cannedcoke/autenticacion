import { CrossfireFrame } from "../frame";
import { UnsupportedOrInvalid } from "./unsupported-or-invalid";
export declare class LinkStatisticsRx {
    /** uint8 - RSSI (dBm * -1) */
    rssiDb: number;
    /** uint8 - RSSI in percent */
    rssiPercent: number;
    /** uint8 - Package success rate / Link quality (%) */
    linkQuality: number;
    /** int8 - SNR (dB) */
    snr: number;
    /** uint8 - rf power in dBm */
    rfPowerDb: number;
    syncByte: number;
    constructor(
    /** uint8 - RSSI (dBm * -1) */
    rssiDb?: number, 
    /** uint8 - RSSI in percent */
    rssiPercent?: number, 
    /** uint8 - Package success rate / Link quality (%) */
    linkQuality?: number, 
    /** int8 - SNR (dB) */
    snr?: number, 
    /** uint8 - rf power in dBm */
    rfPowerDb?: number, syncByte?: number);
    static get payloadSize(): number;
    get frameType(): 28;
    get crossfireFrame(): CrossfireFrame;
    static fromFrame(frame: CrossfireFrame): LinkStatisticsRx | UnsupportedOrInvalid;
}
//# sourceMappingURL=link-statistics-rx.d.ts.map